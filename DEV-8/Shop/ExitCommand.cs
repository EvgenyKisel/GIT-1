using System;
using System.Collections;

namespace Shop
{
   /* class ExitCommand : Commands
    {
        const string EXIT = "exit";

        public override double DoCommand(string command, ArrayList list)
        {
            if (command.Equals(EXIT))
            {
                return Double.NaN;
            }
        }*/
    
}